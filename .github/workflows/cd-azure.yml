name: Deploy to Azure
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging
      dockerImageTag:
        description: 'Docker image tag to deploy'
        required: true
        default: 'latest'
        type: string
      appName:
        description: 'Azure App Service name'
        required: true
        default: 'autobutlerdemo'
        type: string
      storageAccountName:
        description: 'Azure Storage Account name'
        required: true
        default: 'autobutlerdemo'
        type: string

env:
  AZURE_RESOURCE_GROUP: autobutler
  ACR_NAME: autobutleracr

concurrency:
  group: cd-azure-${{ inputs.environment }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Azure CLI
        run: |
          curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
          az version

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_SERVICE_PRINCIPAL_DEPLOY }}

      - name: Build version from git tag
        id: version
        run: |
          VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "dev-${{ github.sha }}")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION"

      - name: Deploy ACR (if not exists)
        working-directory: cd/azure
        run: |
          # Check if ACR exists
          if ! az acr show --name ${{ env.ACR_NAME }} --resource-group ${{ env.AZURE_RESOURCE_GROUP }} &>/dev/null; then
            echo "ACR does not exist, deploying..."
            az deployment group create \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --template-file azuredeploy.acr.json \
              --parameters azuredeploy.acr.parameters.json
          else
            echo "ACR already exists, skipping deployment"
          fi

      - name: Login to Azure Container Registry
        run: |
          az acr login --name ${{ env.ACR_NAME }}

      - name: Build and push Docker image
        working-directory: cd/azure
        run: |
          docker build \
            -t ${{ env.ACR_NAME }}.azurecr.io/autobutler:${{ inputs.dockerImageTag }} \
            -t ${{ env.ACR_NAME }}.azurecr.io/autobutler:${{ steps.version.outputs.version }} \
            --build-arg VERSION=${{ steps.version.outputs.version }} \
            -f Dockerfile \
            ../..

          docker push ${{ env.ACR_NAME }}.azurecr.io/autobutler:${{ inputs.dockerImageTag }}
          docker push ${{ env.ACR_NAME }}.azurecr.io/autobutler:${{ steps.version.outputs.version }}

      - name: Deploy App Service (if not exists)
        id: deploy-app
        working-directory: cd/azure
        run: |
          # Check if App Service exists
          if ! az webapp show --name ${{ inputs.appName }} --resource-group ${{ env.AZURE_RESOURCE_GROUP }} &>/dev/null; then
            echo "App Service does not exist, deploying..."
            az deployment group create \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --template-file azuredeploy.json \
              --parameters \
                appName=${{ inputs.appName }} \
                storageAccountName=${{ inputs.storageAccountName }} \
                dockerImageTag=${{ inputs.dockerImageTag }}
            echo "deployed=true" >> $GITHUB_OUTPUT
          else
            echo "App Service already exists, skipping ARM deployment"
            echo "deployed=false" >> $GITHUB_OUTPUT
          fi

      - name: Update container image (if app exists)
        if: steps.deploy-app.outputs.deployed == 'false'
        run: |
          echo "Updating container image to ${{ inputs.dockerImageTag }}..."
          az webapp config container set \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ inputs.appName }} \
            --docker-custom-image-name ${{ env.ACR_NAME }}.azurecr.io/autobutler:${{ inputs.dockerImageTag }}

      - name: Restart App Service
        run: |
          az webapp restart \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ inputs.appName }}

      - name: Get App Service URL
        id: app-url
        run: |
          URL=$(az webapp show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ inputs.appName }} \
            --query defaultHostName -o tsv)
          echo "url=https://$URL" >> $GITHUB_OUTPUT

      - name: Deployment Summary
        run: |
          echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: ${{ inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "- **App Name**: ${{ inputs.appName }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Docker Tag**: ${{ inputs.dockerImageTag }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Version**: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **App URL**: ${{ steps.app-url.outputs.url }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Action**: ${{ steps.deploy-app.outputs.deployed == 'true' && 'New deployment' || 'Image update' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🚀 Deployment completed successfully!" >> $GITHUB_STEP_SUMMARY

      - name: Logout from Azure
        if: always()
        run: az logout
